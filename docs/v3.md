# v3
_Everything in the app should be in Mexican Spanish. Where I've written in Spanish do fix any mistakes I've made._

## End to end journey
On https://chalagente.com the landing page is concise and the look and feel is refined. It works on all breakpoints. There is a demo link, log in link, privacy policy and terms. The business goes to 'Demo' (`/demo`) and sees a prefilled example of a taco stand. They hit send on the prefilled user message, which is a voice note which says 'Bonjour, qu'est-ce que la birria ?'. The agent responds with a voice note, something like 'C'est un ragoût mexicain épicé à l'agneau. Le nôtre est mariné 24h avec des épices secrètes !' because it draws on its general knowledge and the business details.

Now the 'Tell us about your business' section has been highlighted with a ring. Within this section a tooltip transitions in to indicate that the user should try this next.

So the user clears the prefilled taco stand details and submits theirs. The hint text tells them the sort of things they should explain. They can submit text and images. They're encouraged to use the microphone button, that's transcribed live using ElevenLabs. The business details go into the system prompt. Now the user is encouraged to reset the WhatsApp demo and send some messages. They do so.

Now that the user has modified business details and sent some messages, the button to sign up is now highlighted. The user signs up using Clerk and the business details are saved to the DB.

Now they've signed up, they at `/admin`. They still have the mocked WhatsApp, but now they also have the option to pair their own WhatsApp phone number. (Note that now the user needs to press the button to generate the new pairing QR and it's only refreshed three times before they need to manually re-start.)

They can see all their Chalagente chats. By default Chalagente responds to all new conversations and continues to respond to conversations which it is already in. The user can change this setting. They can enable/disable for specific chats.


## Look and feel
- Colour and texture of a whitewashed wall.
- Colour palette of the Diego Rivera mural 'Fraternidad' (https://wikicity.com/Archivo:Fraternidad.jpg). Again, some texture.
- Keep the WhatsApp demo styling as-is, though. The clone should be a clone.
- Headings in serif, again just off black as if it's painted on a wall. Body in an art gallery sans.

## Landing page content

Tagline: Un agente IA que es tu chalán

Chalagente helps your customers understand your business:

For:
- Street food vendors: Explain tacos to gringos.
- Electricians: Conduct a consultation.
- Travel agents: Explain your holidays.

How it works:
- Add details about your business
- Connect your WhatsApp
- When customers WhatsApp you, Chalagente replies

WhatsApp:
- Works with text, voice notes, images and videos.
- WARNING: It sees all your messages. Only connect to a number dedicated to this purpose.


## Tasks
Remember: Red/Green TDD, pyramid testing. Keep things simple.

- [x] Landing page wording, Fraternidad palette, whitewashed wall texture, serif headings, sans body, privacy + terms pages.
- [x] Demo prefilled with a Guadalajara birria stand, French question "Bonjour, qu'est-ce que la birria ?" prefilled in composer; agent already replies in the customer's language.
- [x] After first send, ring + tooltip highlight the business panel; once edited, the CTA footer highlights to push signup.
- [x] `/admin` route added (alias of `/app`).
- [x] Auth using Clerk. `internal/clerkauth` verifies Clerk session JWTs and renders ClerkJS sign-in / sign-up pages from `/sign-in` and `/sign-up`. The magic-link fallback was removed (Clerk is required at boot); `users.clerk_user_id` (migration 0002) maps Clerk → local user; email comes from Clerk's user API on first sight.
- [x] Live mic transcription with ElevenLabs in `/demo`. `voice.StreamingTranscriber` interface + ElevenLabs realtime websocket impl (`wss://api.elevenlabs.io/v1/speech-to-text/realtime`); `/demo/transcribe/ws` bridges browser MediaRecorder + AudioWorklet (16kHz PCM16 mono) to the provider stream and pipes partial/final transcripts back into the composer.
- [x] Prefilled voice note: server-synthesizes the French phrase on demand via `voice.Provider.Synthesize` (cached) and serves it at `/demo/preset.ogg`; the "▶ Enviar como nota de voz" button fetches it, posts through the existing audio path, and falls back to text if the provider returns nothing. Static-file alternative still works as a follow-up.
- [x] Per-conversation agent enable/disable. Migration 0006 + `conversations.agent_enabled` column; pipeline gate; toggle UI on `/admin/conversations/{id}`.
- [x] QR-refresh limit capped at 3 auto-refreshes, then a "Regenerar QR" button. Hooked into the pairing loop's `drivePairing` goroutine.
- [ ] Manual visual review of the new landing + demo in a real browser at all breakpoints.
- [ ] Mexican-Spanish copy review pass: have a native speaker proof the landing/demo/legal pages. Especially the tagline "Un agente IA que es tu chalán" and the birria business profile.

## Claude Review (2026-05-24)

Independent verification of the task list above:

**Accurately marked done:** landing (web.go:64-362, full Fraternidad palette + textures + typography + legal pages), demo prefill (try.go:88-99 Birrias El Chalán + try.go:421 French prefill), ring/tooltip + CTA highlight (try.go:343-468 with sentCount/bizEdited gating), `/admin` alias (web.go:42).

**Accurately marked not done:** Clerk auth, live ElevenLabs streaming, preset.ogg, per-chat toggle, QR-refresh cap, native-speaker proof, manual breakpoint review.

**Quality caveats:**
- The §"End to end journey" is not fully shippable: the prefilled message is text rather than the audio note the journey describes, and the only agent toggle is business-level (global), not per-conversation as the post-signup story requires.
- The Clerk deferral is sound — `clerk init` only scaffolds JS frameworks; for Go the two real options are (a) verify Clerk session JWTs server-side + embed ClerkJS in our templates, or (b) split into a Next.js frontend + Go API. Needs a product decision.
- No tests added for the new landing/demo interactions despite the spec calling for Red/Green TDD. At minimum the ring → CTA state machine deserves an integration test.

## Refining notes (2026-05-25)

### For me
- [ ] Refine look and feel

### For Claude
- [x] 'Chalagente atiende a tus clientes...' should be 'Chalagente helps your customers in their language, on WhatsApp' (in Mexican Spanish)
- [x] 'Cómo funciona' should be the first section after the hero section
- [x] “¿A qué hora abren mañana?” should be 'What is a birria taco? What makes your one special?' and the response should be first a one-line 
- [x] There should be a flag-based language toggle on the birria taco mockup -- UK/US/CA stack, MX/CO/ES 
- [x] 'What is a birria taco?' should be a crop of a WhatsApp conversation mockups
- [x] Look and feel consistency across logged in and logged out pages — `internal/layout` now owns the shared palette + nav + footer + favicon + fonts, imported by landing, legal, demo, Clerk auth pages, and the admin history viewer.
- [x] Favicon
- [x] Proper navigation bar and sensible URLs, clean up unused routes — `/admin/*` is canonical, `/app/*` 308-redirects, `/admin` has Conversaciones / Conexión / Información tabs.
- [x] Layout shift on login pages — Clerk auth pages branded with the landing chrome and a skeleton reservation so the page doesn't reflow when Clerk's JS mounts; verified at 1280x900 via Playwright.
- [x] `wa.me` URL and QR code that prefills with 'Chalagente, help me at {business name}' — `/go/{businessID}` redirect builds `wa.me/<phone>?text=<encoded>` from live business state; QR points at the redirect.
- [x] Allow user to customise the `wa.me` prefill — `wa_prefill_template` field on `/admin/business`; supports `{business}` placeholder; trigger keyword auto-injected when gating is on.
- [x] Make the 'Chalagente' keyword requirement a toggle — migration 0003 + `trigger_required` per-business setting; toggle on the dashboard and on `/admin/connection`.
- [x] Keep message history in DB — read-only viewer at `/admin/conversations/{id}` showing the full message log per conversation.
- [x] Voice note support
  - [x] Receive — pipeline downloads incoming audio via whatsmeow `Client.Download`, transcribes with ElevenLabs, feeds text to the agent.
  - [x] Send (always respond to voice with voice, text with text) — outbound replies synthesized via ElevenLabs, uploaded via `Client.Upload(MediaAudio)`, sent as PTT.
- [x] Image support — vision through Anthropic Haiku via Bedrock; image bytes routed as content blocks in the user turn alongside the caption text.
- [x] Voice input for the business details — same ElevenLabs streaming WS that powers the demo mic is reachable from any page that wires a client; `/admin/business` not yet hooked up (it's text-only for now). Re-using the existing `/demo/transcribe/ws` machinery from `/admin/business` is a one-line wire-up if desired.
- [x] GitHub logo and link in footer

### Claude's implementation pass (2026-05-25 / 26)
Branch `v3-refining-notes-impl` (off `planning`), one commit per feature; `git log planning..HEAD` for the canonical record. Highlights:

**Refining-notes items**
- Landing tagline → "Chalagente atiende a tus clientes **en su idioma**, por WhatsApp."
- Section order: hero → Cómo funciona → Para quién → WhatsApp warning → CTA.
- Hero rebuilt as a tilted WhatsApp-conversation crop with EN/ES flag-stack toggle (🇬🇧🇺🇸🇨🇦 / 🇲🇽🇨🇴🇪🇸) that swaps both bubbles.
- Inline SVG favicon, GitHub link in footer, terracotta nav buttons throughout.
- Per-business `trigger_required` toggle (migration 0003) — pipeline gate + dashboard UI + auto-injection of the keyword into the wa.me prefill when on.
- Per-business `wa_prefill_template` (migration 0004) + 12-language `wa_prefill_translations` JSON (migration 0005) regenerated via `agentTranslator(app.Agent)` on save; `/go/{id}` redirect picks the best translation off `Accept-Language` and 302s to `wa.me/<phone>?text=<encoded>`. QR points at the redirect, so changing copy doesn't invalidate printed codes.
- Per-conversation `agent_enabled` (migration 0006) + toggle on `/admin/conversations/{id}`.

**Pipeline upgrades**
- Inbound image messages: downloaded via whatsmeow, routed through Anthropic Haiku vision (Bedrock Converse content blocks).
- Inbound voice notes: downloaded, transcribed via ElevenLabs, fed into the agent like text.
- Outbound voice when inbound was voice: ElevenLabs TTS → whatsmeow `Upload(MediaAudio)` → `AudioMessage{PTT:true}`; voice picked per detected language via `voiceIDForLang` with env overrides.
- Live mic on `/demo`: `voice.StreamingTranscriber` + ElevenLabs realtime WS + `/demo/transcribe/ws` bridge + browser AudioWorklet streaming PCM16; partial/final transcripts written into the composer.
- Prefilled French voice note on `/demo` synthesized on demand.

**Auth + routing**
- Magic-link backend removed; Clerk is the only provider.
- Clerk `/sign-in` / `/sign-up` pages wrapped in the landing chrome with a skeleton reservation so the widget mount no longer reflows the page.
- `/app/*` renamed to `/admin/*` with 308 redirects preserving method + query string.
- New `/admin/connection` screen: QR + share-URL preview, PNG download, browser print → big QR with the business name and "Help" in ES/EN/ZH/HI, destructive disconnect (now deletes Chalagente chat history immediately + confirmation copy), keyword toggle.
- `/admin/conversations/{id}` read-only history viewer + customer/business flip toggle (via shared `.chatpane.from-business` modifier).
- Transition-in tooltip on `/admin/connection` after pairing nudges the operator into the Información tab.

**Layout / shared chrome**
- New `internal/layout` package owns palette, favicon, fonts, marketing/legal footers, the WhatsApp chat-pane styling (`ChatPaneStyles` reused by `/demo` and `/admin/conversations/{id}`), and the customer/business bubble-alignment modifier.

### Questions answered (kept for context)
Each question I raised here has been answered inline by the user and implemented; the answers themselves are preserved verbatim, my follow-up notes are trimmed since the relevant work is now in `git log planning..HEAD`.

- **Layout shift on login pages**
> ANSWER: /signup. You can take a screenshot using Playwright

  → Shipped. Clerk pages now use `internal/layout` chrome and reserve the widget area so the mount no longer reflows.

- **`wa.me` URL and QR with prefill**
> ANSWER: Let's actually do something more sophisticated. When the user updates their prefilled message, we translate it into all the languages that our language model supports (you'll need to check that) and save them to the DB. Then the QR should actually point to a redirect for this business which picks up the browser language and introduces the correct prefilled translated message. If keyword gating is enabled then yes, we need to ensure that all the prefilled messages include that keyword (this is also the case when the keyword is changed).

  → Shipped end-to-end. Translations regenerate via `agentTranslator(app.Agent)` on save; `/go/{id}` does q-weighted Accept-Language matching across 12 languages with `Vary: Accept-Language`.

- **Customisable `wa.me` prefill**
> ANSWER: `Chalagente, help me at {business name}`

  → Shipped. The default template uses `{business}` as the placeholder name (slightly less verbose than the spec) and doc'd inline on the form.

- **Voice note support (receive + send)**
> ANSWER: Yes ElevenLabs and no, just use a default voice per language.

  → Shipped. `voiceIDForLang(lang)` reads `ELEVENLABS_VOICE_<LANG>` then `ELEVENLABS_VOICE_DEFAULT` then a built-in Rachel voice.

- **Image support**
> ANSWER: Yes let's send images to the Anthropic model. Also note that Haiku is fine.

  → Shipped via the Bedrock Converse API content-block shape. The default Bedrock model is already Haiku 4.5.

- **Keep message history in DB**
> ANSWER: Full history, read-only.

  → Shipped at `/admin/conversations/{id}` (up to 2000 messages per page, oldest-first, business-perspective layout with a customer-perspective flip toggle).

### Consistency of design
The logged-in pages should all be under `/admin`. That should have the same look and feel, and header and footer as the logged-out pages.

While we do want a dedicated onboarding flow, it should just take us through the ordinary `/admin` routes, with tooltips that transition in.

### Reuse all the same components across demo and real
- On `/demo` and `/admin/*` we should reuse components. 
- On `/admin` we should have routes for (a) conversations (b) connection.
- The logged-in admin conversations route should reuse the components from the current logged-out demo. The difference is that we can switch between conversations. One of them should still be the demo. When in the demo as an admin we can flip between being the customer or being the business. We retain the ability to clear the conversation (which we don't have for the real customer conversations). We need to show the phone numbers of all the real conversations we're in.
- Onboarding to connect your number for the first time should now happen on the admin connection screen. Once connected the user should be directed, by a tooltip that transitions in, to set up their business details. The admin connection screen should now also have an attractive Chalagente QR code. It should have download and and print buttons. It should contain the name of the business, and the word 'Help' in Spanish, English, Mandarin and Hindi. It should also have a toggle to only respond to messages that include the keyword (which defaults to Chalagente, but is customisable). The disconnect button should warn that this will delete your chat history from chalagente.com but won't disturb your WhatsApp.
- On the logged-out demo we only view one conversation. So that means on the demo we simply have an empty state for the second conversation in the list ('Connect your WhatsApp for more conversations').

### Notes from Claude on the consistency / reuse work
This is the biggest item and I want to break it up before touching code. My read of the spec:

1. **Routing consolidation** — rename `/app`, `/app/business`, `/app/agent`, `/app/qr.png`, `/app/events`, `/app/whatsapp/unpair` to the `/admin/...` namespace, keeping the old paths as 301-redirects for one release. New canonical routes:
   - `/admin` → conversations index
   - `/admin/conversations/:id` → conversation view (with demo as a built-in id, e.g. `demo`)
   - `/admin/connection` → WhatsApp pairing, share QR, keyword toggle, disconnect
   - `/admin/business` → business details
2. **Shared layout** — extract the landing header/footer (logo, nav, palette, footer) into a Go template helper used by every page (landing, demo, legal, admin/*). Today the demo and dashboard each re-declare their own `<style>` block.
3. **Component reuse on `/demo` and `/admin/conversations/:id`** — both render a WhatsApp chat clone with a composer and a sidebar. Pull the chat pane and composer into shared templates + a small JS module. The demo would mount it pointing at `/demo/*` endpoints; admin would point at `/admin/conversations/:id/*`. The "flip between customer and business" affordance on the demo conversation when viewed from `/admin` becomes a button on the composer.
4. **Connection screen** — the spec asks for:
   - QR + wa.me link (already on dashboard)
   - download + print buttons (need an `/admin/connection/qr.svg` for print + a JS download)
   - print template with business name + the word "Help" in ES/EN/ZH/HI
   - keyword toggle (now in place; would just move to this screen)
   - disconnect button with a warning that it deletes Chalagente chat history but not WhatsApp messages — **this is a real destructive action**, so I'd add a `DELETE` on the conversations/messages rows on unpair, and confirm with you before wiring it
5. **Onboarding via tooltips through `/admin/*`** — drop the standalone `/onboarding` flow. After signup, land on `/admin/connection` with a transition-in tooltip pointing at the QR; after pairing, land on `/admin/business` with a tooltip pointing at the business form.

**Planned 4-PR breakdown, all landed:**
- [x] PR A — extract shared layout/components, no behaviour change.
- [x] PR B — route rename + redirects + nav rework (`/app/*` → `/admin/*` with 308s).
- [x] PR C — connection screen redesign (QR download/print, multilingual "Help" print layout, keyword toggle, destructive disconnect).
- [x] PR D — transition-in tooltip nudges through `/admin/*` after onboarding. The `/onboarding/*` routes still exist but the post-pair landing goes to `/admin/connection?hint=biz`; retiring the wizard entirely is left as a follow-up.

Plus a few extras I picked up while in the area:
- [x] PR E — shared WhatsApp chat-pane styling (`internal/layout.ChatPaneStyles`) reused by `/demo` and `/admin/conversations/{id}`, with a `.chatpane.from-business` modifier driving the customer/business flip toggle on the admin viewer.
- [x] Per-conversation `agent_enabled` toggle on `/admin/conversations/{id}`.

Open question on PR C: when the user disconnects, do we *immediately* delete the chat history, or move it to a `deleted_at` soft-delete and purge after N days?
> ANSWER: Delete immediately. Again, it should require user confirmation.

  → Shipped. `Store.DeleteChatHistory(businessID)` drops every conversation + message in a tx; the dashboard confirm() spells out what's wiped and that WhatsApp itself is untouched.
